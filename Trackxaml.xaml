<Page x:Class="Авторассылка.Trackxaml"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
     xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
     TextElement.Foreground="{DynamicResource MaterialDesignBody}"
     TextElement.FontWeight="Regular"
     TextElement.FontSize="13"
     TextOptions.TextFormattingMode="Ideal"
     TextOptions.TextRenderingMode="Auto"
 
     FontFamily="{DynamicResource MaterialDesignFont}"

      
      xmlns:local="clr-namespace:Авторассылка"
      mc:Ignorable="d" 
     d:DesignHeight="344" Background="#7fd4e7" d:DesignWidth="600"
      Title="Trackxaml">

    <Grid>
        <DataGrid  FontFamily="Arial" CanUserAddRows="False" CanUserReorderColumns="False" CanUserResizeColumns="False" CanUserDeleteRows="False" x:Name="dg" Background="#7fd4e7" Margin="0,40,0,0"/>
        <ComboBox  FontFamily="Arial"  x:Name="cb" materialDesign:HintAssist.Hint="Выбрать ранее" Margin="10,10,474,0" VerticalAlignment="Top" SelectionChanged="cb_SelectionChanged" />

    </Grid>
</Page>
